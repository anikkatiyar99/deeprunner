# Application
APP_ENV=development
HOST=0.0.0.0
PORT=8000

# Service endpoints used by the API container
OPENSEARCH_URL=http://opensearch:9200
REDIS_URL=redis://redis:6379/0

# Optional future settings for auth / tenancy / index naming
DEFAULT_TENANT_ID=default
DOCUMENT_INDEX_PREFIX=documents
